# 2026-09-15：bypass残余阻塞分类学研究

> 用户问 yolo 设 bypassPermissions 后为何仍不停阻塞（附 browse-rs 内联 python 被拦标本）；claude-code-guide 代理对官方七页文档加 changelog 全量取证，S029 追记残余阻塞分类学。本篇记流水与自省。

## 流水

1. **定位与取证**：先核仓内覆盖（S029 正是 bypassPermissions 专题，已含模式取值序、2.1.257 项目层忽略、一次性 dangerous 框）；标本文案直接点名 `permissions.blockReadsOutsideWorkingDirectories` 读块分析闸（非权限规则层）；派 claude-code-guide 取官方口径（permission-modes、permissions、settings、settings-reference、mcp、sessions、sandboxing 七页加 CHANGELOG 2.1.2xx 扫描）。
2. **分类学落档（S029 追记）**：残余阻塞八类（读沙箱读块、显式 ask 规则、灾难删除安全网、folder trust、MCP 双键条件、跨会话消息与托管安全框、hook 不被跳过、resume 不还原）；项目层静默降级路径（auto 盖 bypass、VS Code 不读项目层）；flag 对 defaultMode 的差集（免疫项目层忽略、resume 覆盖、-p 转拒绝、受保护路径写覆盖）；changelog 无未结回归。**顺带证伪 S029 旧记一条**（resume 沿用模式，官方 sessions 文档相反），随批更正。
3. **hst 落点评估**：yolo full 三键已盖一次性框与 MCP 审批（双在场条件）；不可消除类与可诊断类分开列；doctor yolo 面候选（项目层 ask/defaultMode 在场、读块键在场、resume 模式回退操作提示）登记 S029 待办待用户裁（候选 D50）。
4. **D50 落地交付**：doctor yolo 面补两项新 warn（yolo.ask 与 yolo.readblock，三层全查）加既有检查修正（local 层读取、项目层 bypass 按 2.1.257 忽略语义：bypass-only 假阳性修正且不构成对用户层遮蔽）；codex 评审 F1 至 F4 全收（F1 撤 yolo.mask 名并入 conflict、F2 死夹具补双 bypass 与 partial-user 断言、F3 修 p=bypass 加 u=acceptEdits 误报、F4 ask 扩用户层与 readblock 口径对齐）；5f9420c 加 fe0c94c 推 main CI 绿，180 单测加 28 集成绿，dogfood 零假阳性。流程观察：mdcharlint 与 heading-scan 本地违规退出码为 0，&& 链拦不住（GOAL 破折号两度靠 CI 兜底），建议下批改脚本退出码对齐 CI（待用户裁）。（2026-09-16 复核改判：两脚本退出码本就 1，观察口径疑 M033 管道吞码同型，REQ-005 已验闭合改判 rejected。）
5. **D51 clock 段与 v1.2.0 封版**：用户两令（第一行加年月日当前时间、去秒保分钟：分钟粒度下渲染事件重绘即活钟）；clock 段入默认第一行行尾（Get-Date 零子进程、{datetime} 与 clock 图标键、冒号转义钉 ASCII）；codex F1 至 F3 全收（example 模板三处漂移同步、R002 占位符清单、区域文化冒号坑）；c563ffa 加 7823698 推 main 后随封版令 v1.2.0（D48 至 D51 攒批）发版：tag 09495e9、run 34933356902 六岗绿、六资产 gnu 族 digest 三方对账全等（win 464bed6f、linux 3301f61e、mac fe18f67e）、镜像 hst/stable curl 200 全复验、herdr 知会 ohmycloud catalog pin 素材。
6. **D52 宿主两报修复**：lan-win 报 init「洗键」，沙箱复现修正归因（BOM 下 init 响亮报错零改写、无 BOM 合并正确，宿主现象系 BOM 致 doctor 假报 missing 被误读，正是其第 2 报的坑）；修复 = 读侧 BOM 容忍（json 与 toml 五路全覆盖）加 yolo full 双面落 blockReads=false（off 对称等值摘）加 doctor yolo.parse 显式报；codex F1 至 F4 全收（F1 抓出 statusline merge_claude 同文件漏修、F2 项目级对称、F3 UTF-16 判 Bad、F4 坏文件行措辞）；dc7cdca 加 31f9ca5 推 main CI 绿，183 单测加 28 集成绿；readblock 真机定位实案的正面价值入档 S029。（后修正：第 2 报「BOM 误读」归因被反例证伪，真凶以逐字节指纹定位为 off 摘除路径产出：见第 7 条。）
7. **D52 反例与指纹定位**：宿主干净无 BOM 文件仍被洗成 permissions={skip:true} 单键加 codex 键未落；Linux 以宿主原样键集重放裸 init 三查键全保（复现失败），转从受损形态反推：{skip:true} 单键与 `hst init --yolo=off` 的 retire 产出逐键一致（D52 新加的 blockReads==false 等值摘恰在同路径），沙箱复现逐字节命中；off 分支不打印 scope=full 与宿主引文矛盾，推定包装脚本串联 off 清理趟，已向宿主取证完整命令行与滚动；S029 案例节归因演进三次修正如实记档（含我方两次错误归因：先疑 init 覆盖、再误判 BOM 误读）。
8. **D52 真凶定案与家目录守卫**：宿主全量 marker 复现给出铁证（flag.yolo=false 加 level=full 排除 off 假说；init.hooks.wrote 阶段三文件 (retired-yolo) 行即退役趟足迹）；我方 Linux 以 root==home 确定性复现逐字节命中：deploy_all 的项目级退役趟在家目录为 cwd 时把用户级文件当项目文件，cmd_init 第 1 步刚落的 yolo 键整批被摘；修复 = same_location 守卫（canonicalize 归一符号链接）root==user_home 时退役趟整组跳过打 init.hooks.warn；回归测试钉键全保加 warn 在位加零 retired-yolo（184 单测加 28 集成绿）。教训入自省。
9. **D52 铁证根修**：codex 加急评审 F1 再抓同型缺口（--project-yolo 家目录路径未罩，复现实锤）；全收 = same_location 提 pathutil 共享（canonicalize 归一）、cmd_init project-yolo 支同罩家目录守卫（off 与 full 与 partial 都跳打 init.warn）、INDEX deploy 与 main 行补 D52 标签；project-yolo=off 于家目录实弹 warn 打点加键全保；be0f42c 加 42ad265 推 main CI 绿，舰队解禁回执已发（含家目录跑 init 见 warn 跳点行属预期口径）。
10. **D53 init 并入状态栏面**：ohmycloud 实测知会（宿主脚本停 09-13 版，init 零触碰状态栏）；链内裁「顺手刷」分支（现状非刻意幂等保护，自备另有 marker）。落码三坑全被评审与测试网抓住：home 形参是 hst 根非用户家（第一版传错）、deploy_script 无条件重写（幂等测试抓出改内容判等）、merge 四家内部读 env 绕测试缝（codex F1：跑测试改真机配置的暗病，显式透传连根拔；bisect 定位唯一残余写入者是 verify live 系 D28 既有设计）。codex F1 至 F3 全收（F2 claude 与 codex 写入判等加四配置 mtime 断言、F3 面扩张舰队知会）；185 单测加 28 集成绿；d04ed1c 加 82fa3e0 推 main CI 绿，舰队回执已发。
11. **文档体系迁移 dev-evo（REQ-001）**：装 project-evo 插件（marketplace 名大小写坑：注册名 projectevo 非仓库名）后按 base-init 存量迁移指导重构本仓：骨架幂等生成、AGENTS 重写五节合同（旧四段留档 guides）、ADR 择要四件（定位与 hook 用户级与 gnu 与镜像腿，指针承接不搬运）、REQ 五件（迁移本身 implemented 加活跃队列四项 draft）、docs/README 地图承接 INDEX、四原语与 INDEX 顶部迁移注记。三坑：rumdl 把 YAML frontmatter title 当 h1 将 ADR/REQ 标题整体降级（MD025 配置 front_matter_title 空串根治）、rumdl fmt 动了跟踪文件先惊后核（删除行系 AGENTS 替换与 diary 禁字替换，零内容损失）、check.py PE-11 与存量档案禁字冲突（五百行历史禁字按 G005 SKIP 先例声明豁免，活跃面零容忍）。迁移不是搬运是重审：历史不回填、活档不搬家。
12. **dev-evo 对齐收尾（ProjectEvo wH 飞轮派发）**：README 更多文档节权威指向改 REQ 与 ADR（PRD 与 INDEX 降为迁移留档注记）；PE-11 豁免从 prose 机制化为 PEVO_CHECK_ALLOW（派发清单五处，实跑发现 proven 存量整目录都带禁字，正则放宽为 diary 四篇加 proven/ 整目录，SKIP 218 处退出码 0）；标准命令入 AGENTS Commands 与 docs/README；全门禁绿。
13. **dev-evo 对齐派发二**：地图补红线注记（diary 与 research 不可裁撤，dev-evo 用户裁定 2026-09-16）；guide 与 guides 双目录并存裁定入地图（G 编号元规范活档与任务指南语义不同不合并）；check.py 带豁免退出码 0（SKIP 218 处）复验。

## 自省

- 归因链三次反转的根因是复现条件不完整：前两轮沙箱都用「全新家目录加独立项目目录」，缺了宿主的真实前置（cwd 即家目录）；铁证来自宿主 marker 行（(retired-yolo) 足迹）而非我方推测：生产 bug 复现要向报修方索取全量 marker 输出，那是执行路径的自白。
- 「设置了 X 还弹」类问题先拆层：标本的拦截文案本身就是机制自白（读块分析闸），比猜测权限模式失效快；仓内既有研究（S029）先定位再扩展，避免重立主题。
- 旧研究断言要敢被新官方文档证伪：S029 的 resume 旧记与现行 sessions 文档相反，当场更正并留痕（研究文档的可维护性在于追记与更正并存）。
