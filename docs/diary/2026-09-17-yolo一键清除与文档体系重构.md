# 2026-09-17：yolo一键清除与文档体系重构

> D55 项目级 yolo 干扰一键清除落地；同日用户令文档即代码体系完整重构（ADR-0006，老文档删除出仓）。

## 流水

1. **五端四机 yolo 现况核查**：逐端查四家用户级键与项目目录（wsl 就地、lan 三端 ssh、宿主 findstr），五端全部用户级 full yolo（claude bypass 加 blockReads=false 加 skip、codex never 加 danger-full-access、kimi yolo、grok always-approve），无任何项目级干扰键在位 `[实证: 2026-09-17 五端配置逐件 grep]`。
2. **REQ-009 落地（D55）**：`hst init --clear-project-yolo [--project PATH]` 项目级干扰一键清除，与两级 yolo 旗标互斥、keys-only、家目录守卫同 D52；干扰面按 D50 分类学（claude 两层 settings 的 defaultMode 任意值加 ask 加 blockReads=true、codex 项目 sandbox_mode 加 approval_policy、kimi 项目 default_permission_mode；allow/deny 与其余键不动）；doctor 三处冲突 CTA 扩指新旗标。yolo.rs 新 clear_project_yolo_interference 加单测（外来 acceptEdits 与 ask 摘、allow/deny 保留、空文件删、幂等）、cli.rs 集成测试；修自踩短路坑（`p.remove(a).is_some() || p.remove(b).is_some()` 左真短路漏摘 ask，单测当场抓住，分立两 if）。184 单测加 32 集成绿、clippy 无新警、aidoc strict 0 `[实证: 2026-09-17 cargo test --locked]`。
3. **文档体系重构开工（ADR-0006，用户令「整个项目没有按 adr 的结构来」「按文档即代码体系完整重构」「老文档都要清退」）**：盘点实证（R002 手维护命令权威 66 文件 172 处提及、src 17 文件约 135 行 /// 指针；根目录五件旧四原语簇；guide/guides 双目录；mistakes 不在规范地图；R010 oma 旧口径）。三问两轮裁定：R002 迁 guides 降权威后升级为全部清退；guide/mistakes 归一后升级为删除出仓；根目录簇全迁 proven 后升级为删除出仓；git 历史即档案。目标态：契约在代码（clap help 加 /// 加 tests）、投影双面（hst --llms 与 aidoc）CI 门禁、why 进 ADR、需求进 REQ、AGENTS 五节索引、使用知识靠新写最小 guides。
4. **清退执行（三笔）**：删根目录五件（INDEX 与 PRD 与 GOAL 与 TODO 与 PLAN）加 references 全系（R001 至 R011）加 guide/（G001 至 G005 加模板）加 mistakes/（M101 至 M107）加 web/ 加 proven/（P 件 47 篇）加 agents-legacy 与 getting-started 占位，104 md 存量；历史档案死链走 md-ref-allow 豁免通道五条（diary 与 research 指向已删件的当时路径），活面提及改指 --llms 与 aidoc。治理面改指：AGENTS 重写 43 行（检索阶梯：--llms 到 aidoc 到 /// 到源码；Must 同步链换 clap 注释加测试加 aidoc 重生）、docs/README 重写终态地图（六目录加写作规范一句话版）、guides/环境事实 新篇承接 5端4机与分发长事实、src 17 文件 /// 指针改指模块文档与集成测试、--llms 手册头改指 clap 与 tests、aidoc 重生 strict 0。diary 09-16 两篇归一（items 22 至 29 并入终态对齐篇）恢复一天一篇。
5. **收口核验**：PEVO 11 过 1 豁免（PE-07 补 REQ-009 trace 键、豁免正则尾斜杠笔误自纠、PE-12 摘 requirements/README 的 PRD 死链）；md 四门禁绿、ref 零断链、184 单测加 32 集成绿 `[实证: 2026-09-17 全门禁实跑]`；终态断言：根 md 恰五件（README、CHANGELOG、ROADMAP、AGENTS、CLAUDE）、docs 恰六目录（adr、requirements、guides、diary、research、aidoc）加 README、references 与 guide 与 mistakes 与 web 与 proven 不复存在 `[实证: 2026-09-17 ls 终态]`。
6. **评审一轮收口（herdr codex hst-codex-review 首班，P0 一加 P1 二加 P2 二加观察四）**：P0-1 自家 heading 门禁必红被我漏报（`## 写作规范（一句话版）` 与 diary 第二批标题带全角括号；PEVO PE-10 只查半角括号故 PEVO 绿而自家红，我终树自检时 heading 扫描跑在 docs/README 重写前，item 5 的「md 四门禁绿」口径不实，本笔更正）；P1-1 adr/README 与 ROADMAP 的 proven 死指针（ref 扫描器只认文件不认目录与 glob，结构性盲区）；P1-2 `.tools/README` 的 R008/R009/G 件指针（`.tools` 在三扫描器 SKIP_DIRS 里，唯一不被门禁覆盖的 md）；P2-3 main.rs G2 注释重复（137 处批改唯一语义损伤）加 tests/cli.rs 四处 R004 改指；P2-4 dev-evo 对照加快照注记并限定舰队 ADR-0006 撞号；O-1 REQ-009 边界句（干扰面 D50 三键，ours 残余归 off）；O-2 clear 分支补 init.scope 与 pretrust 落行不再静默丢 --pre-trust；O-3 CTA 钉句加家目录守卫集成测试；O-4 ADR-0005 追注失效句、ADR-0006 P 编号句修、CHANGELOG 补 2.1.0 里程碑、.gitignore 摘 web 三行。
7. **ark 跨工位派单（统一到用户级）收口：doctor 家目录守卫修复**：ark 报 codex/kimi lan 四端项目级混布；实查为测量伪影（`--project $HOME` 把用户配置同时当项目层读，项目分支抢占判级；干净项目下五端均 user level 且 ok `[实证: 2026-09-17 lan-ubuntu 干净项目 doctor codex/kimi 双 user level]`），暴露真缺陷是 doctor 缺 D52 同型守卫。修：diagnose 里 root 为家目录时 claude/codex/kimi 三面 yolo 项目层置 None 走用户级分支；回归钉 home_root_doctor_reads_yolo_as_user_level；185 单测加 32 集成绿。舰队无需迁移（五端用户级 full 本就在位）。
8. **二轮 CONFIRM（附 G 两件）与末笔**：二轮回执 CONFIRM 可推（十项处置逐条核过），附 G1（yolo.ask 与 yolo.readblock 与 parse 三层循环没吃家目录守卫，root 即家时同文件报 project 加 user 双行；修法 claude 层清单化，home root 跳过同体 shared 层，185 加 32 复绿）加 G2（CHANGELOG v2.1.0 条目错落 09-16 节；拆 2026-09-17 独立节并按体例正序）；笔数勘误：当时区间 92250c7 至 c25bc13 实为七笔（我口误报七后又称六，两次皆虚），计数以 git rev-list 区间实数为准不自数（与 09-16 五笔非六同型，二连犯）；.tools/README 的 M023 悬空编号按 M/P 历史标记口径保留（评审观察件）。
9. **三轮观察件 O-1 收口（hooks.retired 家目录同型伪影）**：push_project_residue 三处（claude 的 shared 加 local、codex 的项目 hooks.json、grok 的项目 ohmyagents-state.json）吃 home_is_root 守卫（ours 判定加 `!home_is_root` 短路），root 即家目录时用户级注册本体不再报项目残留；实弹 `--project $HOME` hooks.retired 行 3 至 0、yolo 双 user level 保持 `[实证: 2026-09-17 ./target/debug/hst doctor --project $HOME]`；home_root 回归钉扩断言。
10. **四轮与五轮（O-1 放行与空钉修）**：四轮 CONFIRM 推 ce3dbbb（评审方正反双向实弹：家目录口径 hooks.retired 3 至 0、普通项目口径负向对照仍照报 3，窄化面与 D52 一致）；新 G 指出我的回归钉是空钉（测试环境无 ours hooks，断言永真）；五轮修真钉（种用户级 bare 形态 ours hooks，评审方反向重放到无守卫旧码上必红实证咬合），落 183c520 推 main 绿。记账补正：本笔与空钉修两笔 diary 漏记当场（四轮五轮回执现补）。
11. **发布流水线批 A 落地（ADR-0007，总台核准单 2026-09-17）**：对齐回执九条差距后总台核准 A 到 C 依序与批 D 两裁（dev 轻岗豁免与 ADR-0004 读序豁免入册、digest 缺省钉死随批）。批 A 三件：`.tools/release.ps1` 本地发布链（版本闸 tag 对 Cargo.toml、测试闸、wsl 交叉 linux 加 win-gnu、lan-mac 实机 ssh 构建克隆于 sha、包形补 README 加 LICENSE 对齐标准、逐包 sha256 边车、三端解包冒烟 --version 对 tag、gh 直发 --latest 禁 draft，`-DryRun` 到冒烟止）；dev-release.yml 瘦身（tag 推送不再触发 CI 构建，CI 面收敛为 docs-gate 加测试岗加 dev 轻岗加播种，播种从 Release 资产拉，stable 走 release published 事件、dev 轻岗链内触发因 GITHUB_TOKEN 建档不发事件）；ADR-0007 立档两裁入册。DryRun 实弹全链绿 `[实证: 2026-09-17 三端构建打包冒烟 hst 2.1.0 三端对 tag、publish 按 DryRun 跳过]`；自踩两坑当场修：打包步本地件路径错（target/ 非 dist/）、后台跑 pwsh 管道吞退出码（改直跑看 RC）。
12. **发布流水线批 A 评审两轮与推绿，批 B 落地**：批 A 八笔（fbb87ce 至 e244520）两轮闸门后推 main，run 35188150149 五岗绿（seed 岗首跑成：从 Release 拉资产加边车 sha256sum -c 自校验加 sync hst/dev/）`[实证: 2026-09-17 gh run view 35188150149]`；评审一轮 F1（publish 通配吃 dist/pkg 暂存目录三件，gh 目录上传 EISDIR 留 draft 残档链）加 G1 至 G5 全收（release 臂 prerelease 闸与 tag 形校验、预检 gh auth 加远端 tag 在位加既有 release CTA、测试闸扩 md 四门禁与 aidoc、win 冒烟默认红加显式豁免开关），二轮 CONFIRM 加新 G（pwsh 原生命令失败不触发 ErrorActionPreference，门禁改逐条 rc 断言并实证咬合）。批 B：seed 播种步改双段制（stable 走 release 事件时先 copy `hst/<bare 版本>/` immutable 段加 --immutable 防同版本异内容覆写加长缓存头，再 sync stable 滚动段；版本段与滚动段分别 lsf 计数报 seed.count 行，零对象即红防半失败）；O5 镜像 zone 缓存疑云已记总台回执。

## 自省

- 「完整重构」的中途升级（迁 guides 到全删）靠两轮快问定死再动手，避免了按第一版批复做完再返工；删除批合成一笔是因为待删件互链，拆开必产生中间态断链。
- 老权威退役的正确姿势不是把 R002 搬个目录，而是让指针全部落到活着的地方（clap、tests、///、投影）；137 处 /// 模板指针是「文档是投影」纪律曾缺席的存量证据，本次一次清零。
