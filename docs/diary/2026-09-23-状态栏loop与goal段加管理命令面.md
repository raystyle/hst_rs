# 2026-09-23：状态栏 loop 与 goal 段加管理命令面

> 用户令两面：状态栏显示本会话 loop 计数加节拍与 goal 文本（裁定：两段分立、进默认第二行、仅本会话口径）；hst 可管理当前会话 loop 与 goal（设置加删除）。REQ-019。另：总台周知「/mnt/d 项目没有上 yolo」经实证定性为会话内许可模式漂移，非部署缺口。

## 流水

1. **数据源定标**：本机无 scheduled_tasks.json 存量，亲手 CronCreate durable 探针落一个再删，拿到 schema 实证：`tasks[]` 含 id、cron、prompt、recurring、createdAt、createdBySessionId、createdByPid、createdByProcStart，prompt 即 goal 文本 `[实证: 2026-09-23 本机探针]`。会话内 Bash 有 CLAUDE_CODE_SESSION_ID 与 CLAUDE_PID 环境变量（管理面的会话归属与属主同源通道）`[实证: 本会话 env]`。
2. **状态栏面**：仿 CTXPROBE 先例新建 PS1_LOOPPROBE（自解析会话 id 与项目根，workspace.project_dir 回落链），SEG_LOOP 与 SEG_GOAL 只做渲染；两段进 DEFAULT_SEGMENTS2 尾部，无任务零噪声；cadence 简单形人性化（×Nm、×1h、@HH:mm，解析不出留空不留残迹）；goal 截 16 字符加省略号；grok 走 -ascii 模板无图标。assemble 门控扩 loop 与 goal。
3. **管理命令面**：`hst loop set <goal> --every Nm|Nh` 或 `--at HH:MM`（二选一，读改写保留外会话与 agent 原生任务）、`list`（全量标 ours 归属）、`del`（id 精确任意、latest 本会话最新、all 本会话全部，外会话不误删）。属主字段同源：createdByPid 取 CLAUDE_PID，createdByProcStart 读 Linux /proc 第 22 字段（liveness 同源形）。会话解析序：显式 --session 加 CLAUDE_CODE_SESSION_ID 加 ~/.claude.json 项目表 lastSessionId（正斜杠键，与 pretrust 同判）。
4. **测试**：loopmgmt 单测四件（回环保留外会话、latest 判据、旗标校验、会话解析三级）；statusline 单测四件（本会话渲染、三态隐藏、cadence 三形加截断、grok ascii）加门控测试扩两断言；cli 集成两件（example 同步锁扩加 set/list/del 回环）。全套 218 单测加 46 集成绿。
5. **实弹**：本仓真跑 set（cron */5 落盘加 kv 回显）、list（ours=true）、del（count=1）；hst init 重部署后直跑部署脚本，第二行渲染 `1×5m` 与 goal 文本 `[实证: 2026-09-23 本机]`。
6. **已知边界**（REQ-019 在册）：session-only 循环在内存，盘上无文件不可见；已在跑会话对盘上外部写入的即时接管面无上游承诺，保守口径 = 重开会话必载入（即时接管未实证，后续可探）；同会话混用 hst 与原生 cron 工具会后写覆盖先写，管理纪律单向。
7. **抢修一笔**：PEVO 残红存量（2026-09-20 diary 一处连接号）按 1751edd 先例补修，PE-11 复绿。
8. **goal 独立命令面**（用户令「hst goal 呢」同日扩面）：`hst goal set/show/clear` 落 loopmgmt 三函数（latest_own_index 与 del latest 同判,同刻取后入者）,改目标不动节奏与属主;show 会话不可解析不受阻返回 present=false。单测一件加集成一件。

## 自省

- del 的 all 分支首版把保留集写反（全保留零删除），单测当场抓获；判据型代码（keep 与 remove）值得写时就配对向测试。
- 「没有上 yolo」的误报定性靠 transcript 证据链（启动 header 为 bypass 加中途漂移记录）而非口头回执，与飞轮回执纪律同构：对方陈述不作数，证据自取。
- 四班 diary 笔只跑 charlint 加 rumdl 两条就提交，heading-scan 漏跑致 6c7397f CI 红（35819992421 docs-gate），与 09-20 diary 记过的红 CI 教训完全同型：**补正笔也是笔，md 四门禁是一条链，docs 笔跑全集不分大小**。当笔补修并在此复记。

## 附：周知与功能单回执位

- 周知（2026-09-22）ledger #1 REQ-016：知悉，候裁不动工。
- 「/mnt/d 项目 yolo 失效」定性：会话内许可模式漂移（arkime 会话当日 14:13 切 plan、14:17 切 default、14:19 切 acceptEdits，settings.local.json 积累 allow 为物证），盘上用户级 yolo 生效，恢复 = 该窗格 shift+tab 切回或重开会话 `[实证: transcript permission-mode 时间线]`。

## 二班：claude 压缩触发阈值检测加设置

> 总台功能单 2026-09-23（用户令：70% 就压缩），ledger issue n6。

1. **配置面实证**：claude-code-guide 双源核（官方文档加本机 2.1.270 二进制反混淆）：百分比阈值唯一面 = env `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE`（parseFloat，0 小于值小于等于 100 百分数刻度；二进制内名 testPctOverride，官方文档无此键，未文档面）；窗口面 = env `CLAUDE_CODE_AUTO_COMPACT_WINDOW`（优先于 settings 键 autoCompactWindow 与 /autocompact 命令与 --autocompact 旗标）；`CLAUDE_CODE_AUTO_COMPACT_THRESHOLD` 不存在，`autoCompactThreshold` 是计算态遥测字段非配置键 `[实证: 2026-09-23 二进制 grep 加反混淆算式]`。阈值算式：有效窗口 = 窗口 减 min(模型输出上限, 20000)；阈值 = min(floor(有效窗口 乘 pct 除 100), 有效窗口 减 13000)；无 pct 时 = 有效窗口 减 13000。
2. **两面落地**：检测面 = doctor 新增 claude compact 检查项（信息型恒 ok、settings 坏损 warn，detail 单行带 pct 加窗口来源加开关加近似阈值）；设置面 = `hst init --compact-pct <1-100|off> [--yes]`（仿 --yolo scoped 旗标先例：预览零写、--yes 落盘、设后回读自证、同值幂等）。新模块 compact.rs（快照读加算式加读改写，复用 yolo 面 BOM 容忍 IO）。
3. **实弹**：本机先预览核验零写，再 `--yes` 落 70，回读 `compact.readback pct=70`，doctor 行 `pct=70 window=1000000(src=env) threshold~686000(by_pct=true)`，settings env 块兄弟键全保 `[实证: 2026-09-23 本机]`。生效时机 = claude 会话启动读 env，已在跑会话下次重开生效。
4. **门禁**：单 222 加集 48 绿；新面 clippy 零告警（本工具链存量 31 条不在本批）；aidoc 25 件干净；md 四门禁加 PEVO 全绿。
5. **已知边界**：pct 键未文档（内部 testPctOverride），claude 升级可能变，compact.rs 模块文档在册；阈值按输出预留 20000 上限近似，模型输出上限小于此值时实际阈值更高。

## 三班：推送前评审一轮修复

> herdr 评审闸门（本仓建格 hst-codex-review 首评，codex deepseek-v4-flash）。五笔批基线 7347c01，CONFIRM 五点加门禁全对账，F 两件加 G 四件。

1. **F1 aidoc 漂移**：d480e0e 给 /// 补 # Errors/# Panics 节后没重跑 cargo aidoc，check strict 实弹退 2（评审方证），main CI 必红。修：aidoc 再生提交。教训：**补文档节也是改 ///，提交前 aidoc 门禁要随批重跑**，我此前「干净」声称在 d480e0e 后已过时。
2. **F2 --every Nh 静默放大**：小时支 `M * * * *` 没把 N 进步进位，6h 实得每小时（舰队会跑六倍频）。修：`M */N * * *` 形，状态栏 cadence 补 ×Nh 人性化，测试断言 */2 进 cron。
3. **G1 至 G4 全采纳**：write_tasks 读改写保顶层兄弟键（version 幸存断言）；compact 旗标与 --pre-trust clap 互斥；README REQ-019 计数对齐；removed 按下标集反推、off 判定 trim、pwsh 截断防劈 UTF-16 代理对。
4. **门禁复验**：单 222 加集 48 绿，新面 clippy 零告警，fmt 干净，md 四门禁绿。
5. **二轮 CONFIRM 加建议级三件即修**：小时位 */N 探针实证（CronCreate 落 7 加 */3 加 * 加 * 加 * 形被调度器接受且解读为 Every 3 hours at :07,即删 `[实证: 2026-09-23 本机]`;到点触发面留舰队使用观察）;del_loops 改 drop 下标集单源驱动 removed 与 kept（字节相同重复任务不再失真）;cadence 文档三处补 ×Nh（探针注释加 EXAMPLE_TOML 加 REQ-019 准则）。

## 四班：总台补正轮

1. **五端铺面实弹**（补正单第 2 项）：linux 加 windows-gnu 本地 release 构建，mac 走 CI 刚重建的 dev release 资产（04:28:35Z,晚于推送与 CI 绿）；四远端推临时件跑 `init --compact-pct 70 --yes` 后清理，wsl 就地同版复验。五端回读全绿：`pct=70 window=1000000(src=env) threshold~686000(by_pct=true)`，且五端都已有 1M 窗口键，阈值全端一致 `[实证: 2026-09-23 五端 ssh 直跑]`。
2. **n6 关单口径**（补正单第 1 与 3 项）：hst 的 ledger 面是只增设计（issue new/list/show,关单与状态推进归 omc 工作台,REQ-010 家族分工在册）;手搓签名客户端发 result 加 status done 双事件属另起炉灶不做。n6 保持 open 留痕,验证材料已齐（配置面实证加 CI 35818219404 success 加本机与五端 70 回读）,omc 工作台可随时落双事件;若要 hst 侧可关单,立 REQ 扩 close 面候裁。
