# 2026-09-23：状态栏 loop 与 goal 段加管理命令面

> 用户令两面：状态栏显示本会话 loop 计数加节拍与 goal 文本（裁定：两段分立、进默认第二行、仅本会话口径）；hst 可管理当前会话 loop 与 goal（设置加删除）。REQ-019。另：总台周知「/mnt/d 项目没有上 yolo」经实证定性为会话内许可模式漂移，非部署缺口。

## 流水

1. **数据源定标**：本机无 scheduled_tasks.json 存量，亲手 CronCreate durable 探针落一个再删，拿到 schema 实证：`tasks[]` 含 id、cron、prompt、recurring、createdAt、createdBySessionId、createdByPid、createdByProcStart，prompt 即 goal 文本 `[实证: 2026-09-23 本机探针]`。会话内 Bash 有 CLAUDE_CODE_SESSION_ID 与 CLAUDE_PID 环境变量（管理面的会话归属与属主同源通道）`[实证: 本会话 env]`。
2. **状态栏面**：仿 CTXPROBE 先例新建 PS1_LOOPPROBE（自解析会话 id 与项目根，workspace.project_dir 回落链），SEG_LOOP 与 SEG_GOAL 只做渲染；两段进 DEFAULT_SEGMENTS2 尾部，无任务零噪声；cadence 简单形人性化（×Nm、×1h、@HH:mm，解析不出留空不留残迹）；goal 截 16 字符加省略号；grok 走 -ascii 模板无图标。assemble 门控扩 loop 与 goal。
3. **管理命令面**：`hst loop set <goal> --every Nm|Nh` 或 `--at HH:MM`（二选一，读改写保留外会话与 agent 原生任务）、`list`（全量标 ours 归属）、`del`（id 精确任意、latest 本会话最新、all 本会话全部，外会话不误删）。属主字段同源：createdByPid 取 CLAUDE_PID，createdByProcStart 读 Linux /proc 第 22 字段（liveness 同源形）。会话解析序：显式 --session 加 CLAUDE_CODE_SESSION_ID 加 ~/.claude.json 项目表 lastSessionId（正斜杠键，与 pretrust 同判）。
4. **测试**：loopmgmt 单测四件（回环保留外会话、latest 判据、旗标校验、会话解析三级）；statusline 单测四件（本会话渲染、三态隐藏、cadence 三形加截断、grok ascii）加门控测试扩两断言；cli 集成两件（example 同步锁扩加 set/list/del 回环）。全套 218 单测加 46 集成绿。
5. **实弹**：本仓真跑 set（cron */5 落盘加 kv 回显）、list（ours=true）、del（count=1）；hst init 重部署后直跑部署脚本，第二行渲染 `1×5m` 与 goal 文本 `[实证: 2026-09-23 本机]`。
6. **已知边界**（REQ-019 在册）：session-only 循环在内存，盘上无文件不可见；已在跑会话对盘上外部写入的即时接管面无上游承诺，保守口径 = 重开会话必载入（即时接管未实证，后续可探）；同会话混用 hst 与原生 cron 工具会后写覆盖先写，管理纪律单向。
7. **抢修一笔**：PEVO 残红存量（2026-09-20 diary 一处连接号）按 1751edd 先例补修，PE-11 复绿。

## 自省

- del 的 all 分支首版把保留集写反（全保留零删除），单测当场抓获；判据型代码（keep 与 remove）值得写时就配对向测试。
- 「没有上 yolo」的误报定性靠 transcript 证据链（启动 header 为 bypass 加中途漂移记录）而非口头回执，与飞轮回执纪律同构：对方陈述不作数，证据自取。

## 附：周知与功能单回执位

- 周知（2026-09-22）ledger #1 REQ-016：知悉，候裁不动工。
- 「/mnt/d 项目 yolo 失效」定性：会话内许可模式漂移（arkime 会话当日 14:13 切 plan、14:17 切 default、14:19 切 acceptEdits，settings.local.json 积累 allow 为物证），盘上用户级 yolo 生效，恢复 = 该窗格 shift+tab 切回或重开会话 `[实证: transcript permission-mode 时间线]`。
- 功能单（自动压缩阈值检测加设置，总台 2026-09-23）：配置面实证进行中，落地另笔记。
