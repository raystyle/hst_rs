# 2026-09-16：dev-evo终态对齐

> ProjectEvo wH 飞轮派发三（dev-evo v0.6.0 封版终态清单自审补齐）。本篇记流水与自省。

## 流水

1. **终态五项自审**：门禁面过（check.py 带豁免退出码 0、PE-11 SKIP）；骨架面过（五节合同、地图、索引一致、红线注记）；档案面补两件（本篇即当日笔、research/README 登记存量 S 件 35 件加沿用注记）；三栈面补一句裁定（bin-only 无公开库面，missing_docs 与 cargo test --doc 不适用，示例走集成测试承载）；全门禁绿后提交。
2. **自评表随回执**：五项四过一补（档案面当日笔与索引原缺已补）。

## 自省

- 终态清单是好的收口工具：逐项可判（过/缺/不适用），把「迁移完了吗」从感觉变成对账；当日 diary 笔与 research 索引两处欠账都是清单逼出来的。

2. **aidoc 强制重构（飞轮派发·第五十九批 ADR-0006）**：撤不适用裁定换强制口径；missing_docs=deny 落地补 /// 162 处（模板生成后手清 lib.rs 毛刺与 crate 级叙事、恢复误删 testenv 模块）；cargo aidoc 出 23 artifact（llms.txt 加 hst_cli 分模块加 api JSON）进 Git，--check --strict 退出码 0 入 Commands；PEVO_CHECK_ALLOW 与自家 mdcharlint SKIP 加 docs/aidoc（渲染格式无开关，真门禁 aidoc --check）；REQ-006 登记 implemented；185 单测加 28 集成绿。
3. **契约注释格式纪律增量（第六十批）**：首句成句改写 286 处（去项名开头，名典加语义句）；# Errors 45 处补齐（clippy missing_errors_doc warn 加 missing_panics_doc 加 missing_safety_doc deny 落地，empty_docs deny 同批）；空行断链 23 处扫净；[ui] intra-doc 链接转义 3 处（其中 1 处是测试夹具字符串误伤即改回）；aidoc 重生成 23 artifact strict 退出码 0；185 加 28 绿。
4. **全平台 SSH 实测矩阵（REQ-007）**：WSL 内 ssh lan-mac 与 lan-ubuntu 实证通（Darwin arm64 加 Linux x86_64），lan-win 22 拒绝（宿主 sshd 未开，通道待启）；.tools/cross-test.sh 落地（linux 与 windows-gnu 本地构建、mac 因 ring C 面无法 Linux 交叉改取 dev release 资产、产物名分立防覆盖、doctor 冒烟容错 block 退出码（环境事实非失败）））；首轮矩阵三端绿（local-linux 加 lan-ubuntu 加 lan-mac，mac 端 doctor blocked=false，linux 端 blocked=true 系 kimi 与 grok trust 环境事实）。
5. **dev-evo 终态对照（第六十一批派发）**：17 篇逐面自评（14 已落、2 不适用 Rust 仓、1 部分=tool-cli-agents）；agent CLI 面八项对照（4 已落、3 裁定不适用有据、1 现有形态足够）；退役三篇全仓零引用免清理；自评表落 guides/dev-evo-终态对照自评.md 存档。
6. **四端矩阵闭合（第六十三批连接姿势）**：lan-win 之谜定案（宿主 sshd 在跑但 mesh IP 拒连，WSL 到宿主恒走 127.0.0.1 回环实证 OK，正是 env-platform 第十节口径）；cross-test.sh 加 run_remote_win（回环 ssh 加 powershell 冒烟），四端矩阵首轮全绿（linux 加 ubuntu 加 mac 加 win）；AGENTS 环境节补连接姿势句。
7. **第六十四批跟进**：AGENTS 补 pwsh 统一载体句（五端 7.6.6、非登录 shell 兜底、存量 bash 不强制迁移）、版本载体权威句（Cargo.toml 唯一、semver 判据、载体外即第二真相）、分发体系句（omc 管分发运维加 ark 管落地验收，自升级三通道在册）；连接姿势句上轮已落（6878432）。
8. **v1.3.0 封版（总台统一封版令）**：版本 1.2.0 至 1.3.0 加 CHANGELOG 里程碑 2026-09-16 节；2388ba6 推 main 绿后 tag v1.3.0，tag run 35049625998 六岗绿；六资产 digest 三方对账全等（win aa2323b0、linux db4b8dd4、mac 88d0825a）；镜像 hst/stable 滚动 curl 200；herdr 回执总台（catalog 滚版对端接）。
9. **开发仓位迁移（总台周知）**：本机 WSL 十仓迁独立 VHDX（D:\wsl_workspace\repos.vhdx 50GB ext4，挂载 /mnt/wsl/repos，快捷 ~/repos；登录自动挂载 WSL-RepoDisk schtask）；本仓新工作根 ~/repos/hst_rs（旧位 /home/ray/hst_rs 过渡一至两天后裁）；AGENTS 跨仓路径已切（b5eed8e）。后续会话与日记以新位为准。
10. **REQ-005 核验改判 rejected**：三环境实证两脚本违规退出码均 1（WSL bash 链拦停、Windows pwsh 7 回环链拦停、CI docs-gate 同命令），git -S 证退出码逻辑初版即有未变；09-15 流程观察的「退出码 0」与 M033 管道吞码同型，不落脚本面，REQ-005 关闭回填；顺手修 mdcharlint 仓外绝对路径 relative_to 崩溃（M065，ValueError 回退绝对路径显示）；另核旧位 /home/ray/hst_rs 工作树干净同步 origin/main，裁撤待裁期到后执行。
11. **REQ-004 闭合（grok 退单排 pwsh 闸门断言）**：default_layout 行为测试补 grok 面（与 D42 kimi 钉同载体），三判据一次绿：同配置并一行、agent 态与 token 绝对值可见（grok-2.1.270:unknown 与 195k/977k）、ASCII 面全行无 Nerd PUA 字形（M046 从字符串面升运行时钉）；codex D40 评审 G2 至此 kimi 加 grok 双钉收口。
12. **总台周知行尾统一 LF**：十仓仓级 core.autocrlf=input 覆盖全局 true，工作树按索引重写内容零变化；本会话两笔先行提交 blob 本就 LF 存储无需回改，后续编辑先重读防旧 CRLF 回写，未提交件落地后自然归一。
13. **REQ-002 闭合（资产名与关键词取参纯函数重构）**：update.rs 四私有纯函数落地（asset_name 加 asset_keywords 加 asset_arch 加 pick_asset_for），host 形只转发 env 常量、host_keywords 并入取参形删除；测试去 cfg! 梯子换六组合全平台表断言（资产名加关键词梯子加 host 零漂移），D47 msvc-only 回落判据从 cfg!(windows) 圈内放出，linux/mac 岗同样可断，codex D47 评审 F3 盲区闭合；aidoc 23 artifact 重生成 strict 0；185 加 28 绿。
14. **REQ-003 闭合（self-update 读序不回环集成断言）**：回退守卫抽命名纯函数 github_fail_falls_back_to_mirror（run 内零行为变化）加三态单测；tests/cli.rs 两条假基址集成断言（First 态不回环：假基址连接拒收加不存在仓双失败，镜像腿恰一次、无 fallback=mirror 标记；Off 态全程无镜像标记）；GitHub 腿判据只看标记不看 detail（实跑恰逢 403 限流即验证鲁棒性）；DefaultFallback 实腿仍真网 e2e 佐证（假基址无法注入硬编码默认基址，加 env face 属过度设计，记档）；本机实跑序列与断言逐字吻合；186 加 30 绿。
15. **总台件一（catalog-seed 资产核验）**：v1.3.0 资产未覆写判定 `[实证: 2026-09-16 gh api digest 与镜像 stable 边车内容双源互证，均等于封版对账记录 win aa2323b0 加 linux db4b8dd4 加 mac 88d0825a，GitHub 6 资产（三包加三边车）齐全]`；catalog pin（win 464BED6F7B8C 等）与两源皆不符，判定 pin 落档错值，回执总台节滚 pin、仓侧不重传（重传才真制造覆写）。
16. **总台件二（装态对账）加两端升级**：WSL 实装 1.2.0 属实，`hst self update --stable` 升 1.3.0，GitHub 403 恰触发 DefaultFallback 回退腿、镜像腿装成（读序实录与 REQ-003 断言同形，D48 回退腿价值再证）；Windows 宿主 C:\Users\ray\.hst\bin\hst.exe 1.2.0（总台报 installed 空，实际 PATH 在装供对账），镜像腿拉 win 包（digest 先验 aa2323b0）替换升 1.3.0。
17. **五端矩阵扩展**：cross-test.sh 纳入第五端 lan-linux mesh（用户令五端验收；ssh BatchMode 实证 Linux x86_64 免密，复用 linux 产物）；AGENTS Commands 与 .tools/README 与 REQ-007 判据同步五端口径；五端实弹轮全绿 `[实证: 2026-09-16 wsl 加 lan-ubuntu 加 lan-linux 加 lan-mac 加 lan-win 五端 --version 全 1.3.0 加 doctor 冒烟（linux 系 blocked=true 系 kimi 加 grok trust 环境事实、mac blocked=false，同 REQ-007 首轮口径）]`；当日用户定正名两件：第一端名 wsl 非 local-linux（矩阵标签与文档同改）、基建原语定名 5端4机（wsl 与 lan-win 同宿主机共一机，全平台测试基建）；本批另踩 cross-test.sh 工作树 CRLF 让 bash 报 pipefail 无效名（行尾统一轮遗留，git add 加重检出归一 LF 后复跑即过，总台行尾周知口径内自然归一）。
18. **状态栏 CPU 调查（用户报 176%/78%）**：脚本本体无缺陷判定：部件隔离全快（pwsh 基线 0.6s、git status 10ms、stdin CopyTo 15ms、假 git 加 rustc 替换不省时）`[实证: 2026-09-16 插桩分相位加假 bin 对照加最小复现]`；慢只在九会话渲染风暴下的系统性放大（单 Test-Path 3.4s、全脚本 7 至 13s、常态 4 至 12 路叠跑自激，渲染墙钟超刷新间隔）`[实证: 2026-09-16 pgrep 采样加三组计时]`；健康态渲染约 1s；缓解主杠杆在并发会话侧，仓内可选产品面（状态栏输出 TTL 缓存包装）涉时钟与上下文新鲜度需用户裁定，未擅动。
19. **codex 评审收口（七笔推 main 前，herdr 右侧 wT:p2 起 codex 9m15s）**：F1 阻断一件全收：守卫函数插进 run 的 # Errors 文档块中间致 doc 孤儿化（clippy missing_errors_doc 新引入、aidoc 投影带病仍绿），文档块归位 run、守卫与新增 mirror_fallback_base 各留己注，aidoc 23 artifact 重生成；同型教训即 M064 前缀锚事故自己踩（Edit old_string 只锚了块尾两行）。观察件收 O1（mirror_fallback_base 抽取给三态单测独立 oracle）、O2（宿主 OS 域守卫恢复）、O3（grok ASCII 判据加宽 is_ascii 覆盖非 PUA nerd 符号加 pwsh 缺位 eprintln 标痕）、O4（REQ-005 CI 腿改推断标）、O5（09-15 diary 假断言尾注改判）、O7（mdcharlint rel 基准改仓根，仓外 cwd 705 假阳实证复验修复）、O8（docs-gate 加 cargo aidoc --check --strict 门禁步）；O6 立 REQ-008 draft（三扫描器缺省根统一）；O9 采信不阻断（diary 随批记档属惯例、CHANGELOG 历史节不回改、M063 双号基线在册）。另自省：本会话验 O7 时自己踩 M033 管道吞码（cmd | tail 后取退出码），当场自纠裸跑重测；clippy 门禁此前用 tail 看尾部漏 warning 行，改 grep 全列。186 加 30 绿。
20. **推 main 与 CI 首弹红复盘**：八笔对齐后推（2421964 至 8122144）；aidoc 门禁 CI 首弹红：cargo-aidoc 钉 nightly-2026-07-07 跑 rustdoc，本机恰装有故本地绿、runner 只有 stable 故红（O8 要堵的「本地绿机器红」反向样本，门禁首班就抓了自己）；5b0392c 补 rustup toolchain install 钉版 minimal 后二弹全绿（docs-gate 加三构建加 release 加 mirror 全 success，dev 资产随滚）。工具自省：gh run watch 的 exit-status 骗过一次（退 0 但实况 docs-gate failure），收尾核状改用 gh run view 的 conclusion 字段。
21. **旧位裁撤执行（用户令）**：终验四项过（工作树 0 脏行、stash 0、HEAD 2fda6a9 在 origin/main 祖先链、独有提交 0）后 rm 旧位 /home/ray/hst_rs（7.3G 释放，无进程占用）；仓位迁移收口，本仓唯一工作根 ~/repos/hst_rs（VHDX）。

## 第二批 skill 面退役与 llms 手册面 v2.0.0 封版

（并入自同日单篇，ADR-0006 起一天一篇归一；用户裁定 D54 有 llms 投影即不需要 skill 面。）

### 流水

22. **裁定与立档**：用户令「有 --llms 就不需要 skill 参数命令」「不再有 hst SKILL 安装」。事实边界先核清：aidoc llms.txt 是库面投影只在本仓、skill 是仓外装机运行时发现面，两层不同构；用户知悉后裁定全删。ADR-0005 立档（c4cba3f）：删 `hst skill` 子命令与 init 项目级 fan-out、不再有任何技能安装面、agent 紧凑说明书以 `hst --llms` 出口（活命令树自适应渲染直打 stdout，不落盘）。
23. **代码面**：skillgen.rs 整删；main.rs 删 Skill 变体与 cmd_skill、retire_user_skill 移入 deploy；deploy.rs 删 COMMAND_MAP 加 skill_md 加 write_skill 加 deploy_kimi_project，deploy_skills 改造为 retire_skills（四目录乘 hst 与 ohmyagents 两名，marker 家族判 ours）加 retire_user_skills（用户级 `~/.claude/skills/` 两名，生成签名「活命令树自适应生成」判 ours），外科式伴生资源保留；`--llms` 旗标加 render_llms/walk/synopsis（复用旧 skillgen 遍历器，输出 llms 风格功能面加命令表加输出契约）。契约破裂升 2.0.0。
24. **文档同步**：R002 skill 行换 `hst --llms` 行、init 行与 yolo 行摘 skill 部署措辞、同步链缩两处（R002 行加 docs README；无生成物需重生）；AGENTS Must 两句改（四处同步缩两处、手改生成物清单换状态栏脚本加 aidoc 面）；dev-evo 对照表发现通道三行改口径（skills 自生成已退役、--llms 双面承载）；ADR-0005 并入 --llms 决策。
25. **验证**：183 单测加 31 集成绿（含新增 llms_flag_prints_compact_manual 与 init 清扫断言翻转）`[实证: 2026-09-16 cargo test --locked]`；aidoc 重生成 22 artifact（skillgen 模块消失）strict 退出码 0 `[实证: 2026-09-16 cargo aidoc --check --strict]`；实弹 `hst --llms` 出手册、`hst skill` 报 unrecognized subcommand、dogfood init 清扫本仓 `.agents/skills/hst` 与用户级 `~/.claude/skills/hst` 双 `(retired)` `[实证: 2026-09-16 ./target/debug/hst init]`；doctor blocked=true 系既有环境项（codex 加 kimi 加 grok 项目信任未种、kimi 登录墓碑、grok token 过期），无本次改动引入的新发现 `[实证: 2026-09-16 ./target/debug/hst doctor]`；md 四门禁加 PEVO 合规（PE-11 活跃面无禁字）。
26. **顺手清债**：当日早前 diary 三处破折号禁字（item 18/19/20）改冒号，PE-11 复绿；独立小笔提交。
27. **封版评审闸门（herdr 右侧 codex，快核转全量）**：回执 F 四加 G 五。F1 用户级清扫漏三家（`~/.agents`、`~/.grok`、`~/.kimi-code` 的 skills 还挂 home-root init 落的 marker 件，且判据分叉：签名判据收不了 marker 件），修法：`skill_md_is_ours` 统一判据（marker 家族或旧静态版或生成签名），用户级清扫面扩为四家 skills 根乘两名，测试补 marker 加 `.agents` 用例，实弹 `hst init` 清三残件 `[实证: 2026-09-16 ./target/debug/hst init 三行 (retired) 后四根全空]`；F2 aidoc 孤儿 `hst_cli/skillgen.md` tracked 残件 git rm（`--check` 只比内容不报孤儿，CI 抓不到的盲区）；F3 根 README 三处 skill 现役措辞；F4 R001 两处加 R002 D29 注记补退役括注。G1 自适应口径精确化（命令表自适应，功能面五条与输出契约是手写概览段）；G2 synopsis 裸旗标取值语义渲染（`[--yolo[=full|partial|off]]`，有枚举值列值集，与 R002 对齐）；G3 INDEX 三行刷新（迁移期留档但代码表自称有效）；G4 legacy 四段指南头部加归档注记；G5 跨仓路径债换新位（`/mnt/wsl/repos/project-evo/plugins/evo-adr/skills/code-kit/scripts/check.py`，旧 D 盘位已裁撤）。门禁复跑：183 加 31 绿、aidoc 22 artifact strict 0、md 四门禁加 PEVO 合规、rumdl MD056 一处（R002 表行裸管道转义）。存量非本批：clippy 四条 dead-code 加 deploy 两条 redundant closure 在基线 d933f85 已在，未扩面。
28. **二轮快核与三轮 CONFIRM**：二轮回 F 一（三处权威文本口径滞后：R002 加 ADR 加 CHANGELOG 仍写「用户级 `~/.claude/skills/` 两名」而行为已扩四根）加 G 两（AGENTS 跨仓行 dev-evo 旧 D 盘位、REQ-001 trace 双死路径），落 2ab4e00（六文件六行：三句口径统一、跨仓行换 `~/repos/project-evo`、trace 修现位、测试名随实改）；三轮 CONFIRM 放行（codex 顺手实跑 trace 命令验证路径活、版本面复核全仓无 1.3.0 残留、笔数勘误五笔非六）。
29. **v2.0.0 封版执行**：五笔推 main（c4cba3f 至 2ab4e00，run 35117207324 六岗绿）；tag v2.0.0（run 35117506746 六岗绿）；六资产 digest 三方对账全等（win a2a8fe80、linux 2b7c8686、mac df83e5cd，本地算加边车加 API 三方一致）`[实证: 2026-09-16 sha256sum 对照]`；镜像 hst/stable 三边车 curl 200 且 digest 与 Release 全等 `[实证: 2026-09-16 env.ohmygh.com/hst/stable/]`；装机侧 `hst self update --stable` 1.3.0 至 2.0.0 滚版成功（update.ok=true），装机态终验 `hst --llms` 出手册、`hst skill` 报 unrecognized、`hst init` 幂等零退役行 `[实证: 2026-09-16 ~/.local/bin/hst]`；herdr 回执总台 ohmycloud（catalog 滚版对端接）。

### 自省

- 「有 X 就不需要 Y」的替代式裁定，先核 X 与 Y 是否同层再动手：本轮 llms.txt 与 skill 受众、分发、版本锚三层都不同，靠先摆事实边界让裁定落在知情的面上，ADR 里把放弃的仓外发现面写成 Consequences 而不是埋掉。
- 退役不是删代码完：在位装机件要有清扫路径（ours 判据加外科式），否则留僵尸生成物；本轮复用 D49 的 marker 家族与生成签名判据，零新判据面。
- 评审闸门真抓到东西：F1（清扫面漏三家加判据分叉）与 F2（孤儿 tracked 生成物，门禁盲区）都是我自验全绿后仍漏的面；「已验面不转嫁复验」写清边界反而让评审方专攻增量，一轮回执的命中率比自查高。
