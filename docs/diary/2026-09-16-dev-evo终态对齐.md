# 2026-09-16：dev-evo终态对齐

> ProjectEvo wH 飞轮派发三（dev-evo v0.6.0 封版终态清单自审补齐）。本篇记流水与自省。

## 流水

1. **终态五项自审**：门禁面过（check.py 带豁免退出码 0、PE-11 SKIP）；骨架面过（五节合同、地图、索引一致、红线注记）；档案面补两件（本篇即当日笔、research/README 登记存量 S 件 35 件加沿用注记）；三栈面补一句裁定（bin-only 无公开库面，missing_docs 与 cargo test --doc 不适用，示例走集成测试承载）；全门禁绿后提交。
2. **自评表随回执**：五项四过一补（档案面当日笔与索引原缺已补）。

## 自省

- 终态清单是好的收口工具：逐项可判（过/缺/不适用），把「迁移完了吗」从感觉变成对账；当日 diary 笔与 research 索引两处欠账都是清单逼出来的。

2. **aidoc 强制重构（飞轮派发·第五十九批 ADR-0006）**：撤不适用裁定换强制口径；missing_docs=deny 落地补 /// 162 处（模板生成后手清 lib.rs 毛刺与 crate 级叙事、恢复误删 testenv 模块）；cargo aidoc 出 23 artifact（llms.txt 加 hst_cli 分模块加 api JSON）进 Git，--check --strict 退出码 0 入 Commands；PEVO_CHECK_ALLOW 与自家 mdcharlint SKIP 加 docs/aidoc（渲染格式无开关，真门禁 aidoc --check）；REQ-006 登记 implemented；185 单测加 28 集成绿。
3. **契约注释格式纪律增量（第六十批）**：首句成句改写 286 处（去项名开头，名典加语义句）；# Errors 45 处补齐（clippy missing_errors_doc warn 加 missing_panics_doc 加 missing_safety_doc deny 落地，empty_docs deny 同批）；空行断链 23 处扫净；[ui] intra-doc 链接转义 3 处（其中 1 处是测试夹具字符串误伤即改回）；aidoc 重生成 23 artifact strict 退出码 0；185 加 28 绿。
4. **全平台 SSH 实测矩阵（REQ-007）**：WSL 内 ssh lan-mac 与 lan-ubuntu 实证通（Darwin arm64 加 Linux x86_64），lan-win 22 拒绝（宿主 sshd 未开，通道待启）；.tools/cross-test.sh 落地（linux 与 windows-gnu 本地构建、mac 因 ring C 面无法 Linux 交叉改取 dev release 资产、产物名分立防覆盖、doctor 冒烟容错 block 退出码（环境事实非失败）））；首轮矩阵三端绿（local-linux 加 lan-ubuntu 加 lan-mac，mac 端 doctor blocked=false，linux 端 blocked=true 系 kimi 与 grok trust 环境事实）。
5. **dev-evo 终态对照（第六十一批派发）**：17 篇逐面自评（14 已落、2 不适用 Rust 仓、1 部分=tool-cli-agents）；agent CLI 面八项对照（4 已落、3 裁定不适用有据、1 现有形态足够）；退役三篇全仓零引用免清理；自评表落 guides/dev-evo-终态对照自评.md 存档。

