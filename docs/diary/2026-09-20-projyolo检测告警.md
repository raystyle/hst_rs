# 2026-09-20：项目级 yolo 干扰检测告警

> 用户裁层一（检测加告警，零改动纯可见化）：hook 期节流轻探项目级 yolo 干扰，命中状态栏升格 proj-yolo! 红字，CTA 指向既有 hst init --clear-project-yolo。REQ-017。

## 流水

1. **裁层与落位**：用户在三层选型（检测告警加 opt-in 自愈加排除静默清除）中裁层一。探针落位偏离令面字句：不入 hook 工具门路径（shim 是零 hst 依赖自包含写端 D28，往 shim 塞检测等于 shell 复制检测集造第二真相，且 hook 门延迟敏感），落状态栏哨兵面（REQ-014 先例）节流 10 分钟跑 `hst yolo check`。
2. **实现**：yolo.rs 检测孪生 `project_yolo_interferences`（与清除同源同键只读，家目录守卫 D52 同判）；`hst yolo check` 子命令（kv 行加 hint 加 marker 落 `~/.hst/state/projyolo/<slug>.json`，slug 同 trace 规则，project 原样透传保与状态栏等值匹配）；状态栏 SEG_HST 哨兵二段（读 marker 廉价每渲染，命中同项目且 2 倍窗新鲜升格 proj-yolo! 红 203，与 no-hook! 的序为写端健康优先；节流 stamp 独占创建与到期刷新，同 G2 形）。
3. **测试**：yolo.rs 单测（检测集加只读面加干净零命中）；cli 集成（kv 行加 marker 加字节原样，HST_ROOT 隔离真家）；statusline 单测两件（形锁加行为：marker 驱动升格与项目不符不升格，探针腿 stub PATH 隔离）。
4. **实弹**：种干扰项目（claude 两层加 defaultMode 加 ask）跑 check 出 hit=true 加键行 `defaultMode,ask` 加 hint 行，marker 落盘带 project 原文；部署后状态栏对同项目升格红字 `[实证: 2026-09-20 本机]`。
5. **已知边界**（REQ-017 在册）：清除后至下一窗（至多 10 分钟）告警滞留；slug 碰撞理论误报面。

## 自省

- 令面说「hook 期」而实做在状态栏面，是本轮最重要的落位判断：hook 路径的架构事实（零依赖 shim 加延迟门）比字面更权威。做实现前先读 shim 头十行避免了一次第二真相事故。

6. **评审快核收口（无 F，G 六件全采纳）**：G1 加 G6（marker 增 real 字段 = canonicalize 形加双 slug 各落一份同容，状态栏 project 与 real 任一等值即命中；评审方实弹三态取证：符号链接漂移与尾斜杠都会断字符串等值且 slug 先断文件名）；G2 裁复合态（实时态有价值时 working/proj-yolo! 并显、unknown 直接替换，色恒红）；G3 命令面记 kv-only；G4 家目录守卫改 same_location 单源加失败语义同判；G5 本篇补记红 CI 教训（见下）。测试扩 real 命中与复合态两断言。
7. **红 CI 教训（评审 G5 补记）**：2026-09-19 diary 回填笔 7ff1307 只跑了 rumdl 加 charlint 没跑 heading-scan，括号标题带病入库致该笔 CI 红（35445607738 failure），隔日本批评审才抓回。教训：docs 笔的「门禁跑全集」纪律对纯回填笔同样适用，回填笔最容易被当成免检小笔，而 md 四门禁是一条链不是任选。

## 二班：ledger 账本集成

> REQ-018，总台令（2026-09-20）：集成 ledger 标准 issue 与 artifact 命令族，替代 issues.ohmygh.com 面，完毕封小版本。

1. **契约提取**：设计稿原稿不在案，改读服务端 worker 实现码（ohmycloud workers/ledger/src/index.ts 加 schema.sql）逐面提契约：repo_id 裸斜杠入路径（REPO_ID_RE 放行）、签名基七行 join 换行、JWK 最小 {kty,crv,x} 导入前净化、事件体 {type,payload,body} 形、投影字段名（issue_n 加 hasResult 加 assignee 等）、has_more 由 before 参数或 more=1 触发在位。读不改（禁令遵守）。
2. **密钥面**：一次性 example 生成密钥对（example 即删）；私钥落 `~/.hst/ledger/ed25519.key`（0600）不进仓不进 argv；公钥 JWK 以字母键序紧凑常量内置 ledger.rs，kid = sha256hex(常量) 运行时算（单一真相）。`hst ledger keygen` 供轮换（写档打印公面不泄 seed）。
3. **实现**：src/ledger.rs 新模块（签名道加五头 POST 加 GET 投影加校验集与服务端同源加 issue 开单/事件/关单链/artifact publish/attest/promote/列表）；issue.rs 整体退役（旧 issues.ohmygh.com 客户端面，家族三件迁入 ledger 且提示改指账本网页面）；main.rs 重写 issue 族（new 加 kind 与 acceptance、list、show、close）加 artifact 族加 ledger keygen，fmtio 三态与 kv marker 面（ledger.event.seq 等）承继。
4. **测试**：ledger 单测六件（签名基向量加 kid 自检加 digest 校验加 kind 集对表加 keygen/load 双通道往返加提示面）；cli 集成三件（本地校验秒红加 keygen 隔离不泄 seed）加旧 issue 测试改造（help 真源注记在组级）。踩坑两枚当场收：base64 引擎名 URL_SAFE_NO_PAD（非 NOPAD）、ed25519 rand_core 路径（OsRng 直接满足）；projyolo 行为测试的硬编码 ts 过新鲜窗成时间炸弹，改运行时钟。
5. **挂档**：总台在册公钥后补实弹三验（开单加列表加 experience 产物）；随后封 v2.6.0（CHANGELOG 注明 issue 面切 ledger）。
