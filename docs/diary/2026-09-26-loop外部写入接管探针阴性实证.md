# 2026-09-26：loop 外部写入接管探针阴性实证

> 用户令：验证 goal 与 loop 设置是否真的生效，诊断功能是否针对 goal 与 loop。结论：盘面加显示面全通过；已在跑会话对盘上外部写入的即时接管实证阴性（09-23 diary 在册开放项闭环为否定）；诊断面（doctor 加 diagnose）对 loop 与 goal 零覆盖，候裁 REQ。

## 流水

1. **探针设计**：本会话（id d9728aeb，属主 pid 658930 活 agent 进程）跑 `hst loop set --at 00:01` 一次性任务（id 41480059），再 `hst goal set` 改写 prompt，回合计结束后 REPL 空闲跨越到点时刻。盘面全绿：落盘 schema 与原生同形（八字段齐）、goal 改写 show 回读一致、list ours=true、部署状态栏脚本直跑本会话 id 渲染 `1@00:01` 加 goal 文本截断 `[实证: 2026-09-26 本会话]`。
2. **触发面阴性**：00:01 到点未触发（REPL 空闲满足开火前提，用户回执确认）；00:02:58 复查文件 mtime 仍为 23:57:55 零改动，任务未被消费也未被清 `[实证: 本会话探针加 stat]`。09-23 diary 第 6 条「即时接管未实证，后续可探」闭环为否定性结论。
3. **取证深化**：agent 进程 `/proc/658930/fd` 持 1 个 inotify fd，fdinfo 展开四 watch；wd:2 = 项目 `.claude` 目录（ino 524289 与 stat 对账），wd:1 与 wd:3 = `~/.claude` 与 `~/.claude/settings.json`（设置热载面），sdev 800040 对应 /mnt/wsl 设备 840。即外部写入事件可送达但不被采纳：运行中调度器的真相在内存注册表（CronCreate 通道），盘上文件是重启装载的持久化镜像，不是活输入通道 `[实证: /proc fdinfo 加 stat inode 对账]`。
4. **口径落码**：`loop.hint` 与 loopmgmt 模块文档从「无上游承诺」升格「实证阴性」，aidoc 重生三件；fmt 加 clippy 加 loopmgmt 单测五件加 cli 集成 filter 绿，commit 30dc467。
5. **诊断面盘点**（用户令第二问）：doctor 本机实跑 77 行（claude 加 codex 加 cpu 加 grok 加 kimi 五族，检查面 hooks 加 statusline 加 yolo 加 compact 加 caps），diagnose 是网关活性面（网关发现加模型清单加 key 活性加前缀缓存），两者对 scheduled_tasks 加 loop 加 goal 零覆盖 `[实证: 源码 grep 零命中加实跑清单]`。候选 REQ：doctor 增 loop 与 goal 健康检查（scheduled_tasks.json 可读性、死属主孤儿任务、外会话任务堆积告警），候裁不动工。
6. **重启装载补触发阳性**（当日闭环）：探针任务 41480059 留盘后用户开新会话 36e0fd07，00:16:24 启动装载即浮出补触发：transcript queue-operation 实证 missed one-shot 以用户确认门浮出，prompt 即 goal 改写后文本，且已从文件自删（mtime 00:16:24、tasks 空）。用户回执「设置的没了」即此消费，非丢失 `[实证: 36e0fd07 transcript 加 stat]`。重启装载路径闭环；recurring 任务装载后触发面当日再闭环：任务 93fd954c（*/10 周期）经会话重启装载进入调度，07:22 前后首火回执进入本会话，原生 CronDelete 自清且盘面同步清空（运行中调度器不认盘上外部删除、原生工具双清写穿，与即时接管阴性互补成完整机制图；首火即自清，多轮持续周期性触发仍循上游 durable 契约）`[实证: 2026-09-26 首火回执加 CronDelete 回执加 loop list 复验]`。
7. **评审闸门与后续布防**：五笔（四实证笔加吸收笔 dc648ef）经 hst-codex-review 两轮快核放行推 main（一轮 CONFIRM 加 G1 双真相表述消歧加 G2 六态补标，吸收后二轮放行），CI run 36160396682 绿；评审格自愈 = 原 ccoe-review 格已退出，工位 tab 右分 40% 新建 codex 驻 home cwd `[实证: gh run list 加 herdr agent list]`。REQ-020 立项（doctor 增 loop 与 goal 健康检查，draft 候裁）。recurring 装载触发探针布防：任务 93fd954c（*/10 周期）经 set 加 goal set 改写带自清指令（首火回执后原生 CronDelete 自清，勿 hst loop del 删盘不停火），下次新会话装载首火即闭环流水 6 的推断项 `[实证: loop list 回读]`。
8. **门禁坑一笔**：PEVO 自检首跑误传 PEVO_CHECK_ALLOW=1（布尔臆断），未带豁免正则表致 140 处误报（aidoc 生成面加四篇存量 diary 全中，形似存量红）；按 docs/README 末节标准命令带豁免表重跑合规（PASS 11 / FAIL 0 / SKIP 1）。教训：AGENTS.md 命令简写「PEVO_CHECK_ALLOW 加 uv run」的取值在 docs/README 末节标准命令原文，跑前先读，勿臆断环境变量形 `[实证: 两次对照运行]`。
9. **REQ-021 类型契约化试点落地**（用户令参考 Grok 社区共识「把契约做成机器可检查物」）：loopmgmt 三处补型：Cadence 枚举替换双 Option 旗标（互斥类型层成立，双给实由 clap conflicts_with 在解析层先拦、双缺归 cmd 层）；Task 结构体 typed 承载（serde default 容忍缺键、flatten 保未知键、错型由静默吞默认改响亮报错）；LoopError 枚举加 code() 稳定短码（CLI 错误面 loop error=bad_every 形，agent 判别不靠字符串前缀）。零新依赖（Display 手写）。全测 224 加 48 绿，clippy 零新增（19 行告警全存量），aidoc strict clean，md 四门禁绿 `[实证: 本机门禁输出]`。推广面（yolo、compact 等模块同型补型）候裁。
10. **评审一轮回执吸收**（hst-codex-review，F1 加 F2 必修加 G1 至 G6，我方逐条先核实再修）：F2 属实（本机复现：`{"tasks":{"weird":1}}` 被 loop set 静默整段覆盖，真数据丢失路径）即修：tasks 键在场而非数组改硬错拒覆写；F1 属实（缺必需字段报错文案把缺失说成 wrong-typed 且不点名行）即修：统一「bad task row at tasks[i]」文案，文档对齐「五可缺三必需」口径，id 不给 default（del 定位）；G1 采纳（EmptyGoalClear 变体保 hst goal clear 指引尾巴）；G2 采纳（行下标进文案）；G3 文档路线采纳（`loop error=<code>:` 前缀明写为 agent 契约，结构化短码字段候裁 REQ-011 在册 draft）；G4 采纳（Task 注释撤「字段序同序」无据断言，明写重排语义）；G5 采纳（缺键补全明写接受变化加单测）；G6 半采纳（corrupt 加 no_session 短码端到端断言补齐；文案收敛不做：现文案对双缺仍准确且被集成测试锁，改动无净收益）。单测 7 到 11 件，cli 断言 3 到 5 面 `[实证: 本机复现加门禁重跑]`。
11. **REQ-020 实装加 G8 修**（二轮回执 G8 点名可选，同 F2 型即修）：write_tasks 顶层非对象由静默重置改硬错（读写两面，`[1,2,3]` 类真数据不再被整段盖掉，单测 g8 件）；doctor 增 claude 族 check=loop 行（REQ-020 闭环）：缺文件或零任务 ok、坏损 warn（顶层非对象/tasks 非数组/行错型同 loopmgmt 判）、Linux 死属主 warn（pid 目录不在、或 procStart 落盘非空且不匹配；pid=0 归属未知与 stat 不可读均不判死）、非 Linux 降 ok 加无判据注记、detail 带 tasks 加 ours 加 foreign 加 dead_owner 对账面。loopmgmt 新增 health() 快照面（LoopHealth 四字段），doctor 只渲染不重复判。单测 loopmgmt 14 件加 doctor 25 件绿。实盘自证：本仓 doctor loop 行报 `tasks=1 ours=1 foreign=0 dead_owner=0`（93fd954c 属主即本会话现役进程 690719，判活正确；死属主态由单测消失 pid 4000000 验证；首探针 41480059 的旧属主 658930 已死但该任务早已被补触发消费，不在盘）（自查纠偏一笔：首版此处误预判 dead_owner=1，落库前经 /proc 直查纠正，探针布于会话重启后属主本就是新进程）`[实证: 本机 doctor 实跑加 /proc 直查]`。
12. **REQ-022 推广裁定与 compact 收尾**（用户令「都做完」）：实测盘点 = String 错误计数（deploy 19 加 yolo 14 加 statusline 13 加 update 11 等，函数级签名计数、剔 fn 指针类型与 I/O 管道 helper，全数直达 error_exit 人面）、bool 元组返回扫描（唯一 (值, 旗标) 冗余形残点 = compact threshold_approx；另 doctor::codex_hooks_sides 返 (bool, bool) 属两独立事实非同类）、Json 载体审查。实改一件：threshold_approx 撤 `(u64, bool)` 元组改 `Threshold { ByPct, Default }` 枚举（bool 与 effective_pct().is_some() 冗余，输出串逐字保形，by_pct 字面单测双形加 cli 集成锁）。裁定两笔：外部属主长 schema 文件（settings、claude.json）保持 raw Json 部分键读改写（设计非弱契约）；存量成熟模块错误面不整批 enum 化（人面直达，agent 判别走 `error=<code>` 前缀契约，新模块循 loopmgmt 先例），循参考共识「不铺满每个 CRUD」。结构化短码独立面仍归 REQ-011 `[实证: 全仓 grep 盘点加全测 234 加 48 绿]`。
13. **ADR-0009 加 REQ-023 herdr 即时生效通道落地**（用户令参考 herdr-cc-loop 工件与 socket API 文档，裁定内嵌 RPC 直发并扩面到 loop/goal 四入口管理族）：受限编排面局部解除 D15（仅 ping 加 agent.prompt 两方法、一连接一请求、无事件流无 spawn/status 面），传输零新依赖（std Unix socket、Windows 管道按文件开 `\\.\pipe\` 前缀映射对齐 herdr 二进制 strings、serde_json 组帧、sha256 请求 id，tokio 家族不进 Cargo.toml）。`--via-herdr <target>` 四入口：loop set 派 CronCreate、loop del 三形派 CronDelete、goal set 派 own-latest 删旧建新保节奏、goal clear 派整任务退役（原生无空 prompt 形，语义差异在册）；节奏翻译单源在 hst，指令自含守卫（只用原生 Cron 工具勿手改盘）；blocked 分流不落 Enter；失败不回落写盘。裸 NDJSON ping 探活通（herdr 0.9.1 协议 22）`[实证: python 直连 ping 加 schema 提取]`；假服务器协议集成锁 schema 与三态 `[实证: cargo test loop_via_herdr]`。目标边界（用户情报对版官方文档）：时间面四入口仅 Claude Code 格（原生 Cron 家族）；codex 无对等 /loop，其 /goal 是 Goal Mode 条件驱动续跑（与 hst goal 的 loop-prompt 语义不同物）；错发 codex 格由目标自报无工具回执 `[经验: 用户情报 2026-09-26，官方文档入口在贴]`。

## 自省

- 09-23 记「后续可探」时若当天多等三分钟即可闭环，开放项当天闭环成本最低；记录开放项时应附最小验证配方（探针形加判定窗），本次补齐。
- 阴性结论的取证层级可复用：行为层（到点未触发）加物证层（mtime 零改动）加机制层（inotify watch 在场但不采纳），三层齐才能把「没生效」说成「看见了不认」，避免把上游未文档化行为误报成缺陷。
- 「设置的没了」初听像丢失，实为补触发消费：一次性任务触发即自删，消失即回执。用户回执先取证再定性，与 09-23「yolo 失效」误报定性同构；阴性结论两小时内被阳性补全，同日闭环开放项的价值再加一条。
