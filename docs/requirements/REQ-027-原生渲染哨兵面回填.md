---
id: REQ-027
title: 原生渲染哨兵面回填
status: draft
priority: should
trace: 待实施后回填
---

# REQ-027:原生渲染哨兵面回填

## Scenario

ADR-0010 状态栏渲染原生化后，pwsh 载体的两道哨兵在生产路径静默下线（评审 G6 实证：同一现场档 pwsh 显示 proj-yolo! 而原生显示 unknown）：REQ-014 hook 注册自愈哨兵（unknown 时探注册面、缺位升格 no-hook!、节流窗内 best-effort 自愈 hst hook init）；REQ-017 项目级 yolo 干扰哨兵（marker 命中同项目且新鲜升格 proj-yolo!、节流窗 hst yolo check 探针）。pwsh 弃用期内两载体并存可回退，弃用期收尾删除 pwsh 前必须回填，否则两特性随载体退役失效。

## Criteria

验收判据,可检验、可勾选:

- [ ] REQ-014 面：statusrender hst 段 unknown 时按 agent 定位注册面文件探 hst-state 干 stem，缺位升格 no-hook!；节流窗（每 agent 1 小时）到期 best-effort 自愈
- [ ] REQ-017 面：projyolo marker 读序与新鲜窗判定、同项目（project 与 real 双等值）命中升格 proj-yolo!、复合态并显（working/proj-yolo! 形）、节流窗 hst yolo check 探针
- [ ] 自愈与探针调用复用 hst 子命令（与 pwsh 同形，spawn 面受控）；渲染路径不被自愈失败阻塞
- [ ] 行为测试同 pwsh 面四件（哨兵升格、节流窗、marker 命中、复合态）移植原生侧
- [ ] 实施后回填 frontmatter 的 trace，状态改 implemented
